(function () {
  'use strict';

  window.CellsPolymer.start({
    routes: {
      login: '/',
      register: '/register',
      home: '/home',
      detail: '/detail',
      // 'movement-detail': '/movement/:id/:label',
    },
  });
})();
